#include "main.h"
/**
 * _abs - checks absolute value
 * @int n : numbers being checked
 * Return: ....
 */
int _abs(int)
{
if (n < 0)
{
int _abs(int);
abs_value = n * -1;
return (abs_value);
}
return (c);
}

