#include "main.h"
#include <stdio.h>

/**
 * add - addition of numbers
 * @int: numbers added
 * Return: Always 0.
 */
int add(int, int)
{
int a, b, c;
int c = a + b;
int add(a, b)
_putchar(c)
return (0);
}

